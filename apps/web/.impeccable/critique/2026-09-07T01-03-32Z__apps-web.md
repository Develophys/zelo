---
target: apps/web
total_score: 28
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 2
timestamp: 2026-09-07T01-03-32Z
slug: apps-web
---
Method: dual-agent (A: design-review sub-agent · B: detector + browser-evidence sub-agent)

## Design Health Score: 28/40 (70% — Good, down slightly from round 8's 29)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Clicking a sector pill on /manager lights up the other three pills and leaves the clicked one off |
| 2 | Match System / Real World | 4 | NR-1/PGR framing, CVV 188, SUS/convênio branching, CRM redaction; only "Pico" reads as analyst-speak |
| 3 | User Control and Freedom | 3 | No way to isolate one sector without deselecting the other three; peer partner can't go off-duty |
| 4 | Consistency and Standards | 2 | Same weekly trend series renders at genuinely different visual scales on desktop vs mobile |
| 5 | Error Prevention | 3 | Excellent k-anonymity logic; undercut by a pre-ticked data-authorization checkbox |
| 6 | Recognition Rather Than Recall | 3 | Médico's own history chart has no printed values |
| 7 | Flexibility and Efficiency | 2 | 18 notifications, one flat list, 4 types, no filter, no grouping |
| 8 | Aesthetic and Minimalist Design | 3 | Disciplined type/color; /peer is ~78% empty at desktop width |
| 9 | Error Recovery | 3 | Excellent error copy; invite-send failure offers no retry |
| 10 | Help and Documentation | 3 | Thorough privacy explainer; nothing explains what the médico's own chart measures |
| **Total** | | **28/40** | **Good** |

## Design Specificity Verdict

Strongly specific: NR-1/PGR framing, CVV crisis line, SUS-vs-convênio branching, CRM-pattern redaction, idiomatic PT-BR throughout. Zero CLI detector findings across 433 files (verified via positive-control test). One confirmed detector false positive (body-level layout-transition, contradicted by direct getComputedStyle sampling), one real-but-invisible-at-mobile-viewport finding (sidebar transition), one real low-severity finding (PHQ-9 progress bar transition), and clipped-overflow-container on manager admin routes confirmed false positive (native <dialog> top-layer rendering is immune to ancestor overflow).

## Priority Issues

[P0] Sector filter pills invert the user's intent on first click — ManagerDashboardPage.tsx's effectiveSelected defaults to all ids, toggleSector removes an id already present, so clicking a pill from "Todos" excludes it instead of isolating it.

[P1] The same trend series renders at genuinely different visual scales across breakpoints — toTrendBarHeights (padded domain) on desktop vs toTrendBars (literal 0-100) on mobile, manager-trend-chart.ts.

[P1] Chat redaction is real but invisible, and the copy overclaims its scope — AnonymizeTextUseCase genuinely redacts the network payload, but the displayed transcript intentionally keeps raw text; "anonimizado antes do envio" is unqualified but only 3 patterns are covered.

[P2] The aggregate-signal consent toggle is pre-ticked with no confirmation feedback — revisits round 8's explicit user decision to default it on; flagged as a tension, not acted on unilaterally.

[P2] The médico's own history chart carries no values; Sinais por setor has no legend — HistoryChartCard.tsx prints no visible percentage, unlike the manager's equivalent chart.

## Persona Red Flags

Casey (privacy-skeptical médico): pre-ticked data-sharing box, then an anonymization promise, then their own CRM sitting unredacted in the transcript one second after typing it as a test.

Alex (gestor under time pressure): clicks UTI to isolate it, watches the other three sectors light up instead while UTI stays grey.

## Minor Observations

- Mobile bottom nav is 6 items across 390px with ~0 gutter left.
- /peer is a single ~180px card in a 900px desktop viewport; no way to go unavailable.
- /peer/settings lacks the density control /manager/settings has.
- /manager/notifications has 4 consecutive identical failed-invite rows with no dedupe or retry.
- /assessment lists MBI-HSS as permanently "em breve" beside two live scales.

## Questions to Consider

- The manager's chart prints a value on every bar; the médico's own chart prints none — why the asymmetry?
- The redactor covers CRM/email/phone; the copy says "anonimizado," unqualified — which promise should the UI actually make?
