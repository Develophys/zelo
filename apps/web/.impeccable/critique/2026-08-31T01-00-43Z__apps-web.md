---
target: apps/web
total_score: 27
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 5
timestamp: 2026-08-31T01-00-43Z
slug: apps-web
---
## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | PeerPartnerInbox cannot express an error state; assessment has no resume |
| 2 | Match System / Real World | 3 | "Voltar ao início" goes to /assessment; manager KPI labelled "burnout" but computed from PHQ-9/GAD-7 severity |
| 3 | User Control and Freedom | 3 | Nine answers lost on refresh; unlink and follow-up are one-tap irreversible |
| 4 | Consistency and Standards | 2 | Doctor and manager shells diverge (overflow nav, sidebar labels, collapse persistence) |
| 5 | Error Prevention | 2 | "Não estou bem" is a one-tap unconfirmed write; consent guard missing on /chat, /assessment, /crisis, /peers |
| 6 | Recognition Rather Than Recall | 3 | Band label rendered with no meaning; PHQ-9 and GAD-7 bands differ silently |
| 7 | Flexibility and Efficiency | 3 | No resume for a backgrounded PWA mid-assessment |
| 8 | Aesthetic and Minimalist Design | 3 | Three CTAs on the crisis offer; six blocks on Home |
| 9 | Error Recovery | 3 | ChatAlerts is best-in-class, but the peer-partner socket has no failure path |
| 10 | Help and Documentation | 2 | No band explanation; manager AI output has no AI disclaimer and exports to PDF |
| **Total** | | **27/40** | **Acceptable (68%)** |

Previous run: 25/40. Cognitive load: 4 of 8 fail (single focus, visual hierarchy, minimal choices, working memory).

## Method note — the detector has been running degraded

`htmlparser2`, `css-select`, `css-tree` and `domutils` are absent from the repo and from the skill directory. Assessment B proved the effect with a control file carrying five known anti-patterns: **2 of 5 caught**; low-contrast, overused-font and dark-glow all missed. Those are the detector's highest-value rules. Every "detector clean" reported during the remediation session was an undercount, not a clean bill of health. Independent contrast computation (200 pairs) was substituted.

## P0

### 1. The crisis offer still promises a psychologist it cannot provide
`CrisisOfferPage.tsx:26` reads "Sim, quero falar com um psicólogo". The destination shows the CVV number — the same number on the previous screen — then asks a SUS/plano question. `request-human-handoff.usecase.ts` documents the handoff as unbuilt. The accept screen was redesigned earlier in this cycle, but the promise on the button that leads to it was never touched.

### 2. A severe score with item 9 = 0 gets no crisis affordance
`score-assessment.usecase.ts:25` derives `riskSignal` from PHQ-9 item 9 alone, and `AssessmentResultPage` gates `RiskSignalCallout` on it. A doctor scoring 24/27 who did not tick the self-harm item sees a screen pixel-identical to one scoring 3/27. Meanwhile `is-concerning-score.ts:7` applies a stricter threshold (>9) to build the manager's aggregate than the individual ever has applied to themselves.

## P1

- **The page title is the smallest heading on every in-app screen.** `AppHeader.tsx:66` renders the only h1 at 15/16px sans while `CardTitle` renders 18px serif. Home, AssessmentSelect, You, Settings, Peers and all seven manager pages have no body heading at all. This is the unpriced cost of the shared-header change.
- **"Não estou bem" is a dead end.** `FollowUpCard.tsx:43` records the answer, the card unmounts, nothing happens. A doctor said they are not okay and the interface deleted the question.
- **`Button` md ships 8px horizontal padding.** `Button.tsx:31` — invisible while `full`, visibly wrong on every `full={false}` button.
- **`PeerPartnerInboxPage` has no error state.** `usePeerPartnerConnection.ts:6` — the state union has no error member and initializes to "connecting", so a dropped socket strands a volunteer on "Conectando..." forever.
- **Accept and decline render the same card.** The user made a difficult choice and the interface answered identically either way.

## P2

- **The doctor sidebar is still unusable at 768px.** `Sidebar.tsx:30` hides labels with `hidden lg:block` and offers only a native `title=`. `ManagerSidebar` had this exact defect fixed earlier in the cycle, with a comment diagnosing it; the fix was never carried across.
- **The accent preference can un-brand the app.** `index.css:226-251` ships teal, indigo and clay (a brown), exposed as equal swatches on the doctor's settings page. Sage is a third of the declared visual direction.
- **Border tokens sit at 1.17–1.74:1** where WCAG 1.4.11 asks 3:1 — most consequentially `TextField`, whose fill is 1.11:1 from the page, making the faint border the sole boundary cue.
- **No resume.** Nine answers live in component state; a backgrounded PWA loses them silently.
- **`ScoreDial` has no accessible sentence** — the most important output in the product reads as four disconnected fragments, and its `/max` clears AA only via the large-text exemption at exactly 24px.

## P3

Dead `QuestionCardSkeleton`; "Email" should be "E-mail" in four places; `AssessmentResultPage:48` label/destination mismatch; bare `<hr>`; arbitrary sizes duplicating tokens; router consent guard missing on four routes; manager AI interpretation exports to PDF with no AI disclaimer.

## What is genuinely strong

Mechanical hygiene is exceptional: 0 hardcoded colours, 0 arbitrary colour values, 0 sub-44px targets, 0 unnamed icon buttons, 0 unmatched focus suppressions, 0 lint/type/boundary failures, 1645 tests passing, universal reduced-motion coverage with a correct `motion-essential` opt-back-in. Several executable guards (`theme-contrast`, `token-pairing`, `focus-visible`) pre-empt findings this audit would otherwise have raised. The crisis-line plumbing, the assessment instrument, and the entry sequence are authored work.
