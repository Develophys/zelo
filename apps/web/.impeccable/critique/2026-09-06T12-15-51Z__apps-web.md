---
target: apps/web
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 5
timestamp: 2026-09-06T12-15-51Z
slug: apps-web
---
Method: dual-agent (A: design-review sub-agent · B: detector + browser-evidence sub-agent)

## Design Health Score: 24/40 (60% — Acceptable)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Sector filter pills all render as "selected" by default — no visible off-state |
| 2 | Match System / Real World | 4 | Idiomatic PT-BR clinical voice throughout |
| 3 | User Control and Freedom | 3 | Review-before-send, resumable drafts; no undo on admin delete |
| 4 | Consistency and Standards | 2 | Mobile trend chart prints %, desktop doesn't; peers deletable, managers aren't |
| 5 | Error Prevention | 2 | Delete confirm never names its target |
| 6 | Recognition Rather Than Recall | 2 | Desktop trend chart has no printed values |
| 7 | Flexibility and Efficiency | 2 | No per-notification read, no date range, no peer availability toggle |
| 8 | Aesthetic and Minimalist Design | 3 | Coherent system; three near-empty screens |
| 9 | Error Recovery | 3 | Error copy names the actual consequence |
| 10 | Help and Documentation | 1 | Nothing defines "par anônimo," the peer countdown, or the dashboard headline metric |
| **Total** | | **24/40** | **Acceptable** |

## Design Specificity Verdict

Genuinely authored: the self-harm PHQ-9 item shows CVV 188 unconditionally; "Apoio" is a permanent nav tab; manager copy uses real NR-1/PGR occupational-health language with an explicit non-compliance disclaimer. Generic slippage: all three personas' Settings screens are the same component; the peer-partner surface still reads as a scaffold next to the médico flow's depth.

Deterministic scan (`detect.mjs --json apps/web/src`, 433 files): 0 findings, exit 0, verified against a positive control.

Browser overlay evidence (14 routes, both personas authenticated): `layout-transition` on `<body>` on all 14 routes (broad global width-transition rule). `clipped-overflow-container` on 3 of 5 manager routes, naming ManagerShell's own wrapper divs — the same element this session's scroll-lock fix changed from `md:`-conditional to unconditional overflow-hidden.

## Overall Impression

The médico crisis/assessment flow is the strongest part of the product. The score held flat because this round found problems in territory rounds 1-4 hadn't opened: dashboard chart legibility, the consent screen's checkbox-like decoration, and the peer-partner experience.

## What's Working

1. Assessment review step (`ScaleAssessmentPage.tsx:133-146`) — every answer editable before submit, "Nada foi enviado ainda."
2. Keyboard-vs-pointer discrimination on question cards (`QuestionCard.tsx:79-81`) — arrow keys don't auto-advance past the self-harm item.
3. Error copy names the consequence, not just the failure, consistently across unrelated files.

## Priority Issues — P1

1. **Manager dashboard trend chart unreadable on desktop.** Bars measured 22-26px inside a 56px well (4px spread hiding a 40%→46% move); mobile prints %, desktop doesn't. `ManagerDashboardPage.tsx:285-330`, `presentation/lib/manager-trend-chart.ts`. → `/impeccable layout` or `/impeccable clarify`.
2. **Sector filter has no "off" state.** All 5 pills render identically filled by default. `SectorPillPicker.tsx:4-9`. → `/impeccable clarify`.
3. **Manager panel's overflow-hidden ancestors may clip dropdowns/popovers.** Detector-named `ManagerShell` wrapper divs on 3/5 manager routes — the element this session's scroll fix changed. Needs a manual check of `SectorMultiSelect`, `BottomSheetMenu`, notification badge popover at each breakpoint. → `/impeccable audit`.
4. **Destructive confirm dialog doesn't name its target.** "Excluir par?" with no name, reachable from both row and bulk delete. `ManagerAdminPeersPage.tsx`. → `/impeccable harden`.
5. **Peers screen's value proposition sits behind the gate it should open.** The "neither side sees the other's identity" reassurance only shows after linking, not before. `PeersPage.tsx:57-82`. → `/impeccable onboard`.

## Priority Issues — P2

1. **Home history chart has no empty state.** Renders bars + a legend for colors that never draw when there's no data yet. `HistoryChartCard.tsx:56-90` (manager's equivalent has `TREND_EMPTY` copy; médico's doesn't).
2. **Médico's overflow nav is an unlabeled arrow.** `ArrowUp` icon with only an `aria-label`, hiding Configurações/Administração/Par anônimo; manager's identical control is labeled "Mais". `BottomNav.tsx:56-69`.
3. **Consent page's decorative checkmarks read as pre-ticked consent boxes.** `aria-hidden` bullets that every sighted user will read as already-checked, on the one LGPD-sensitive screen in the app. `ConsentPage.tsx:56-60`.
4. **Mobile header logo tap target is 36×36** against the product's own stated ≥44×44 minimum. `AppHeader.tsx:35,37`.
5. **Collapsed sidebar label token is 11px** (`--text-nav-rail`), visible in the 768-1024px window the manager dashboard targets. `app/index.css:117`, `Sidebar.tsx:37`, `ManagerSidebar.tsx:52`.
6. **Notifications have no dedup or per-item read**, and "Convite aceito" (good) / "Falha no envio" (bad) share the same amber warning pill. `ManagerNotificationsPage.tsx:77`.
7. **Peer-partner inbox has no identity/greeting, availability toggle, history, or visible response countdown**, even after this session's redesign gave it a proper header and states. `PeerPartnerInboxPage.tsx`.
8. **Admin tables wrap emails mid-word at 1280px** while the Setores column sits empty — a column-sizing problem.
9. **Chat composer nests as a card inside a card** per the detector. Minor visual nesting worth a look.
10. **`/assessment/result` visited with no state silently redirects** with no explanation to the user.

## Priority Issues — P3 (polish)

1. Managers can't be deleted (peers can), with no stated reason on screen.
2. No self-service password change exists anywhere; admin panel only offers "Redefinir senha de X".
3. Manager dashboard's trend window is hardcoded and inconsistent between cards ("últimas 6 semanas" vs "(4 semanas)"), no date-range control.
4. Chat action tray's collapse chevron reads as a rendering glitch before its padded 44px hit area is discovered. `ChatActionTray.tsx:34`.
5. "Falar com uma pessoa real" shortens to "Pessoa real" at the exact moment escalation matters most.
6. Global `transition: width` fires on `<body>` across all 14 routes — likely broader than intended, worth scoping down.
7. `layout-transition` flagged on `aside.hidden` is likely a false positive (element is `display:none`).

## Persona Red Flags

**Jordan (first-timer)**: Onboarding through consent is good, then breaks twice fast — an empty home chart with a legend for colors that never draw reads as broken, not empty; and the consent screen's decorative checkmarks will be read by every sighted user as pre-ticked consent boxes, on the one LGPD-sensitive screen where that's the worst possible reading.

**Sam (accessibility-dependent)**: Well served overall (skip link, `role="status"`, full `aria-label` coverage) but three concrete gaps against the product's own AA commitment: 36×36 mobile logo tap target, 11px collapsed-sidebar labels in the tablet window the manager dashboard targets, and no text-size/high-contrast control despite four accent colors and two corner-radius options existing.

## Minor Observations

- 4 duplicate "Falha no envio do convite" notifications for the same address, no dedup.
- Global `<body>` width-transition detector finding across all 14 routes.
- Dark mode is genuinely well-executed across médico and manager surfaces; nothing broke there.

## Questions to Consider

- What if the manager dashboard withheld "46% sinais de burnout" (an undecided metric per PRODUCT.md) until it's actually defined, leading instead with response rate and the k-anonymity guarantee?
- What if the peer-partner inbox's primary object were an availability switch ("Disponível até 06:00") instead of a passive connected-status card?
- What if the médico's home screen led with the last check-in's result instead of an empty chart, which only earns its place at check-in #3?
